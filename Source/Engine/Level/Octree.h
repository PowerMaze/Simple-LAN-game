#pragma once

/*
class COctreeNode 
{
public:

	std::vector<CPolygonWorld> polygons;
	std::unique_ptr<COctreeNode> childs[8];

	Vector3D vCenter;
	float fSize;

public:
	COctreeNode();
	~COctreeNode();
	void Load(CFileName& strFileName);
	void Save(CFileName& strFileName);

	void processNode(COctreeNode* tree);

	bool testPointInPolygon(Vector3D& vPoint);

	void Clear();
};
*/